export const heroScript = "window.ScrollTrigger = window.ScrollTrigger || {\n  config(){}, create(){}, update(){}\n};\nwindow.gsap = window.gsap || {\n  registerPlugin(){},\n  timeline(opts = {}) {\n    const chain = {\n      from(){ return chain; },\n      fromTo(){ return chain; },\n      to(){ opts.onComplete && opts.onComplete(); return chain; }\n    };\n    return chain;\n  },\n  quickTo(){ return () => {}; },\n  from(){},\n  to(target, vars = {}) { vars.onComplete && vars.onComplete(); },\n  set(){},\n  ticker:{ add(){}, lagSmoothing(){} }\n};\ngsap.registerPlugin(ScrollTrigger);\n\nconst REDUCED = window.matchMedia('(prefers-reduced-motion: reduce)').matches;\n\n/* ── 1. Intro overlay ── */\nif (!REDUCED) {\n  const introTl = gsap.timeline({\n    onComplete() { const el = document.getElementById('intro'); if (el) el.remove(); }\n  });\n  introTl\n    .from('#intro-wordmark',  { opacity:0, duration:0.45, ease:'power2.out' }, 0.1)\n    .fromTo('#intro-circle',\n      { strokeDashoffset:603 },\n      { strokeDashoffset:0, duration:0.65, ease:'power2.inOut' }, 0.1)\n    .to('#intro-wordmark', { y:-56, opacity:0, duration:0.35, ease:'power2.in' }, 0.82)\n    .to('#intro',          { opacity:0, duration:0.28, ease:'power2.in' }, 0.95);\n}\n\n/* ── 2. Live MVD time ── */\n(function(){\n  const el = document.getElementById('mvd-time');\n  if (!el) return;\n  const fmt = new Intl.DateTimeFormat('en-UY', {\n    timeZone:'America/Montevideo',\n    hour:'2-digit', minute:'2-digit', second:'2-digit', hour12:false\n  });\n  const update = () => el.textContent = fmt.format(new Date());\n  update(); setInterval(update, 1000);\n})();\n\n/* ── 3. Cursor ── */\n(function(){\n  if (window.__bhCursorInitialized) return;\n  window.__bhCursorInitialized = true;\n  if (window.matchMedia('(hover:none)').matches) return;\n  const dot = document.getElementById('cursor');\n  if (!dot) return;\n  /* quickTo drives left/top so mix-blend-mode works correctly */\n  dot.style.transform = 'translate(-50%,-50%)';\n  const xTo = gsap.quickTo(dot, 'left', { duration:0.35, ease:'power3' });\n  const yTo = gsap.quickTo(dot, 'top',  { duration:0.35, ease:'power3' });\n  window.addEventListener('mousemove', e => { xTo(e.clientX); yTo(e.clientY); }, { passive:true });\n  document.querySelectorAll('[data-hover],a,button').forEach(el => {\n    el.addEventListener('mouseenter', () => dot.classList.add('is-hover'));\n    el.addEventListener('mouseleave', () => dot.classList.remove('is-hover'));\n  });\n  window.addEventListener('mouseleave', () => dot.style.opacity = '0');\n  window.addEventListener('mouseenter', () => dot.style.opacity = '1');\n})();\n\n/* ── 4. Lenis smooth scroll (desktop only — touch devices use native scroll) ── */\nlet lenis = null;\nconst isTouchDevice = window.matchMedia('(hover:none)').matches;\nScrollTrigger.config({ ignoreMobileResize: true });\nif (!REDUCED && !isTouchDevice) {\n  lenis = new Lenis({ lerp:0.085, wheelMultiplier:0.9, smoothWheel:true });\n  lenis.on('scroll', ScrollTrigger.update);\n  gsap.ticker.add(t => lenis && lenis.raf(t * 1000));\n  gsap.ticker.lagSmoothing(0);\n}\n\n/* ── 5. SplitType heading reveals ── */\nif (!REDUCED && typeof SplitType !== 'undefined') {\n  document.fonts.ready.then(() => {\n      const targets = document.querySelectorAll(\n      '.studio__head,.problem__head,.system__head,.services__head,.cases__header h2,.offer__head,.custom__head,.process__head,.faq__head,.contact__head,.home-head,.contact-preview__head'\n    );\n    targets.forEach(el => {\n      const split = new SplitType(el, { types:'lines,words' });\n      el.querySelectorAll('.line').forEach(l => {\n        const w = document.createElement('div');\n        w.style.cssText = 'overflow:hidden;display:block;';\n        l.parentNode.insertBefore(w, l); w.appendChild(l);\n      });\n      gsap.from(split.words, {\n        y:18, opacity:0, stagger:0.015, duration:0.7, ease:'power3.out',\n        scrollTrigger:{ trigger:el, start:'top 85%', toggleActions:'play none none none' }\n      });\n    });\n  });\n}\n\n/* ── 6. Reveal observer + stat counters ── */\nconst io = new IntersectionObserver(entries => {\n  entries.forEach(e => {\n    if (!e.isIntersecting) return;\n    e.target.classList.add('is-visible');\n    io.unobserve(e.target);\n    const numEl = e.target.matches('.stat') ? e.target.querySelector('[data-count]')\n                : e.target.matches('[data-count]') ? e.target : null;\n    if (numEl) countUp(numEl);\n  });\n}, { threshold:0.18 });\ndocument.querySelectorAll('.reveal').forEach(el => io.observe(el));\n\nfunction countUp(el) {\n  const target = parseInt(el.dataset.count, 10);\n  const dur = 2000, start = performance.now();\n  function tick(now) {\n    const k = Math.min(1, (now - start) / dur);\n    el.textContent = Math.round(target * (1 - Math.pow(1-k,3)));\n    if (k < 1) requestAnimationFrame(tick);\n    else { el.textContent = target; el.classList.add('is-done'); }\n  }\n  requestAnimationFrame(tick);\n}\n\n/* ── 7. FAQ accordion ── */\n(function(){\n  const items = document.querySelectorAll('.faq__item');\n  if (!items.length) return;\n  const setPanel = (button, panel, open) => {\n    button.setAttribute('aria-expanded', String(open));\n    if (REDUCED) {\n      panel.style.maxHeight = open ? 'none' : '0px';\n      return;\n    }\n    const inner = panel.querySelector('.faq__panel-inner');\n    panel.style.maxHeight = open && inner ? `${inner.scrollHeight}px` : '0px';\n  };\n\n  items.forEach(item => {\n    const button = item.querySelector('.faq__button');\n    const panel = item.querySelector('.faq__panel');\n    if (!button || !panel) return;\n    setPanel(button, panel, false);\n    button.addEventListener('click', () => {\n      const isOpen = button.getAttribute('aria-expanded') === 'true';\n      setPanel(button, panel, !isOpen);\n    });\n  });\n\n  window.addEventListener('resize', () => {\n    items.forEach(item => {\n      const button = item.querySelector('.faq__button');\n      const panel = item.querySelector('.faq__panel');\n      if (button && panel && button.getAttribute('aria-expanded') === 'true') {\n        setPanel(button, panel, true);\n      }\n    });\n  }, { passive:true });\n})();\n\n/* ── 8. Section color washes ── */\nconst cssVar = name => getComputedStyle(document.documentElement).getPropertyValue(name).trim();\n[\n  { trigger:'#servicios-preview', color:() => cssVar('--surface') || '#EAF2FF' },\n  { trigger:'#proyectos-preview', color:() => cssVar('--bg') || '#F7FAFF' },\n  { trigger:'#offer-preview',    color:() => cssVar('--bg') || '#F7FAFF' },\n  { trigger:'#contacto-preview',  color:() => cssVar('--surface') || '#EAF2FF' },\n].forEach(({ trigger, color }) => {\n  if (REDUCED || !document.querySelector(trigger)) return;\n  const wash = () => typeof color === 'function' ? color() : color;\n  const base = () => cssVar('--bg') || '#F7FAFF';\n  ScrollTrigger.create({\n    trigger, start:'top 55%', end:'bottom 55%',\n    onEnter:     () => gsap.to(document.body, { backgroundColor:wash(), duration:1.2, ease:'power2.out' }),\n    onLeave:     () => gsap.to(document.body, { backgroundColor:base(), duration:1.2, ease:'power2.out' }),\n    onEnterBack: () => gsap.to(document.body, { backgroundColor:wash(), duration:1.2, ease:'power2.out' }),\n    onLeaveBack: () => gsap.to(document.body, { backgroundColor:base(), duration:1.2, ease:'power2.out' }),\n  });\n});\n\n/* ── 8. HERO: SVG-first scroll-scrubbed animation (anime.js) ── */\n(function heroSetup() {\n  const section = document.getElementById('hero');\n  if (!section) return;\n  if (REDUCED) return;\n\n  const svg = document.getElementById('bh-svg');\n  if (!svg) return;\n  if (typeof anime === 'undefined') {\n    section.classList.add('hero-static');\n    return;\n  }\n\n  const DESKTOP_VIEWBOX = '0 0 1600 900';\n  const MOBILE_VIEWBOX = '210 30 1180 840';\n  const clamp01 = v => Math.max(0, Math.min(1, v));\n  function cubicBezierProgress(t, x1, y1, x2, y2) {\n    const cx = 3 * x1;\n    const bx = 3 * (x2 - x1) - cx;\n    const ax = 1 - cx - bx;\n    const cy = 3 * y1;\n    const by = 3 * (y2 - y1) - cy;\n    const ay = 1 - cy - by;\n    const sampleX = u => ((ax * u + bx) * u + cx) * u;\n    const sampleY = u => ((ay * u + by) * u + cy) * u;\n    const sampleDX = u => (3 * ax * u + 2 * bx) * u + cx;\n    let u = clamp01(t);\n    for (let i = 0; i < 5; i++) {\n      const dx = sampleX(u) - t;\n      const d = sampleDX(u);\n      if (Math.abs(dx) < 0.0001 || Math.abs(d) < 0.0001) break;\n      u = clamp01(u - dx / d);\n    }\n    return sampleY(u);\n  }\n  function frameSvgForViewport() {\n    const mobile = window.innerWidth < 820;\n    svg.setAttribute('preserveAspectRatio', 'xMidYMid meet');\n    svg.setAttribute('viewBox', mobile ? MOBILE_VIEWBOX : DESKTOP_VIEWBOX);\n  }\n  frameSvgForViewport();\n  window.addEventListener('resize', () => frameSvgForViewport(), { passive: true });\n\n  /* Phase HUD */\n  const phLabel = document.getElementById('ph-label');\n  const phIndex = document.getElementById('ph-index');\n  const phNum   = document.getElementById('ph-num');\n  const phName  = document.getElementById('ph-name');\n  const phSub   = document.getElementById('ph-sub');\n\n  const PHASES = [\n    { num:'FASE / 01', name:'Plano',        sub:'Todo gran sitio empieza con un plan' },\n    { num:'FASE / 02', name:'Estructura',   sub:'Mapeamos cada sección antes de escribir código' },\n    { num:'FASE / 03', name:'Esquema',      sub:'El wireframe guía el diseño' },\n    { num:'FASE / 04', name:'Diseño',       sub:'Damos vida al plano con tu marca' },\n    { num:'FASE / 05', name:'Desarrollo',   sub:'Código limpio, carga rápida y resultado preciso' },\n    { num:'FASE / 06', name:'En todos lados', sub:'Funciona perfecto en cada pantalla' },\n  ];\n  let curPhase = -1;\n  const rows = phIndex ? phIndex.querySelectorAll('.ph-index__row') : [];\n\n  function setPhase(i) {\n    if (i === curPhase) return;\n    curPhase = i;\n    if (!phLabel) return;\n    if (i < 0) {\n      phLabel.classList.remove('is-vis');\n      phIndex && phIndex.classList.remove('is-vis');\n      rows.forEach(r => r.classList.remove('is-active'));\n      return;\n    }\n    const ph = PHASES[i];\n    phNum.textContent  = ph.num;\n    phName.textContent = ph.name;\n    phSub.textContent  = ph.sub;\n    phLabel.classList.add('is-vis');\n    phIndex && phIndex.classList.add('is-vis');\n    rows.forEach(r => r.classList.toggle('is-active', +r.dataset.phase === i));\n  }\n\n  /* Pre-compute stroke lengths for everything we'll draw */\n  const drawLines = svg.querySelectorAll('.bh-line');\n  drawLines.forEach(el => {\n    if (typeof el.getTotalLength === 'function') {\n      try {\n        const len = el.getTotalLength();\n        if (len > 0) {\n          el.style.strokeDasharray  = len;\n          el.style.strokeDashoffset = len;\n        }\n      } catch(e){}\n    }\n  });\n  const frameRect = document.getElementById('frame-rect');\n  if (frameRect && frameRect.getTotalLength) {\n    const len = frameRect.getTotalLength();\n    frameRect.style.strokeDasharray  = len;\n    frameRect.style.strokeDashoffset = len;\n  }\n\n  /* Layout constants (in SVG viewBox space 1600×900) */\n  const PAPER = { cx: 800, cy: 450, w: 1120, h: 540, l: 240, r: 1360, t: 180, b: 720 };\n\n  // Map wireframe groups → section keys\n  const WF_MAP = [\n    { id: 'wf-header', key: 'navigation' },\n    { id: 'wf-hero',   key: 'hero' },\n    { id: 'wf-image',  key: 'content' },\n    { id: 'wf-cards',  key: 'cards' },\n    { id: 'wf-footer', key: 'footer' },\n  ];\n\n  /* The reveal clip — opens horizontally from the center */\n  const clipRect = document.getElementById('centerRevealRect');\n  clipRect.setAttribute('x', PAPER.cx);\n  clipRect.setAttribute('y', PAPER.t);\n  clipRect.setAttribute('width', 0);\n  clipRect.setAttribute('height', PAPER.h);\n  function syncRevealClip(time) {\n    if (time < 1700) {\n      clipRect.setAttribute('x', PAPER.cx);\n      clipRect.setAttribute('y', PAPER.t);\n      clipRect.setAttribute('width', 0);\n      clipRect.setAttribute('height', PAPER.h);\n      return;\n    }\n    if (time < 3400) {\n      const k = cubicBezierProgress((time - 1700) / 1700, 0.5, 0, 0.2, 1);\n      const w = PAPER.w * k;\n      clipRect.setAttribute('x', PAPER.cx - w / 2);\n      clipRect.setAttribute('y', PAPER.t);\n      clipRect.setAttribute('width', w);\n      clipRect.setAttribute('height', PAPER.h);\n      return;\n    }\n    if (time < 3800) {\n      const k = (time - 3400) / 400;\n      const w = PAPER.w + (1800 - PAPER.w) * k;\n      const h = PAPER.h + (900 - PAPER.h) * k;\n      clipRect.setAttribute('x', PAPER.cx - w / 2);\n      clipRect.setAttribute('y', PAPER.cy - h / 2);\n      clipRect.setAttribute('width', w);\n      clipRect.setAttribute('height', h);\n      return;\n    }\n    clipRect.setAttribute('x', PAPER.cx - 900);\n    clipRect.setAttribute('y', PAPER.cy - 450);\n    clipRect.setAttribute('width', 1800);\n    clipRect.setAttribute('height', 900);\n  }\n\n  /* ── Initial state ── */\n  anime.set('#paper-group', { translateX: 0, translateY: -900, rotate: '-2deg', scale: 0.96, opacity: 0 });\n  anime.set('#roll-left',   { translateX: 800, translateY: 450, opacity: 1 });\n  anime.set('#roll-right',  { translateX: 800, translateY: 450, opacity: 1 });\n  anime.set('#stage-shadow',{ opacity: 0, scaleX: 0.4 });\n  anime.set('#grid-group',   { opacity: 0 });\n  anime.set('#frame-detail', { opacity: 0 });\n  anime.set('#wf-tags',      { opacity: 0 });\n  WF_MAP.forEach(({id}) => { anime.set('#' + id, { translateX: 0, translateY: 0, rotate: '0deg', opacity: 1 }); });\n  anime.set('#browser-ui', { opacity: 0 });\n  anime.set(['#bx-chrome','#bx-dot1','#bx-dot2','#bx-dot3','#bx-addr','#bx-addr-text','#bx-content'], { opacity: 0 });\n  anime.set(['#mon-bezel','#mon-neck','#mon-base','#mon-shadow'], { opacity: 0 });\n  anime.set('#bx-mobile', { opacity: 0 });\n  anime.set('#ph-notch',  { opacity: 0 });\n  const stageGroup = document.getElementById('stage-group');\n  const paperSheet = document.getElementById('paper-sheet');\n  const monBezel = document.getElementById('mon-bezel');\n  if (stageGroup) stageGroup.setAttribute('transform', 'translate(0 0) scale(1)');\n\n  /* ── Master timeline ── */\n  const tl = anime.timeline({ autoplay: false, easing: 'easeInOutCubic' });\n\n  /* PHASE A — DROP (0–1400) */\n  tl.add({\n    targets: '#paper-group',\n    translateY: [-900, 0],\n    rotate: ['-2deg', '0deg'],\n    scale: [0.96, 1],\n    opacity: [0, 1],\n    duration: 1400,\n    easing: 'cubicBezier(0.2, 0.7, 0.2, 1)',\n  }, 0);\n  tl.add({\n    targets: '#stage-shadow',\n    opacity: [0, 0.10], scaleX: [0.4, 1],\n    duration: 1200, easing: 'easeOutExpo',\n  }, 200);\n\n  /* PHASE B — SETTLE (1400–1700) */\n  tl.add({\n    targets: '#paper-group',\n    translateY: [0, -6, 0],\n    duration: 360,\n    easing: 'easeInOutSine',\n  }, 1400);\n\n  /* PHASE C — UNROLL (1700–3400) */\n  // Center reveal expands outward\n  const reveal = { p: 0 };\n  tl.add({\n    targets: reveal, p: [0, 1],\n    duration: 1700,\n    easing: 'cubicBezier(0.5, 0, 0.2, 1)',\n    update: () => {\n      const w = PAPER.w * reveal.p;\n      clipRect.setAttribute('x', PAPER.cx - w / 2);\n      clipRect.setAttribute('width', w);\n    }\n  }, 1700);\n\n  // The two halves of the tube roll outward as the paper reveals.\n  // They shrink in width (scaleX -> 0.08) as they reach the edges, simulating\n  // the paper \"rolling up\" tightly into the end caps.\n  tl.add({\n    targets: '#roll-left',\n    translateX: [800, PAPER.l + 25],\n    duration: 1700,\n    easing: 'cubicBezier(0.5, 0, 0.2, 1)',\n  }, 1700);\n  tl.add({\n    targets: '#roll-right',\n    translateX: [800, PAPER.r - 25],\n    duration: 1700,\n    easing: 'cubicBezier(0.5, 0, 0.2, 1)',\n  }, 1700);\n\n  // Once paper is open, expand the clip beyond paper bounds and fade the rolls.\n  const clipExpand = { w: PAPER.w, h: PAPER.h };\n  tl.add({\n    targets: clipExpand,\n    w: [PAPER.w, 1800],\n    h: [PAPER.h, 900],\n    duration: 400, easing: 'linear',\n    update: () => {\n      clipRect.setAttribute('x', PAPER.cx - clipExpand.w / 2);\n      clipRect.setAttribute('y', PAPER.cy - clipExpand.h / 2);\n      clipRect.setAttribute('width',  clipExpand.w);\n      clipRect.setAttribute('height', clipExpand.h);\n    }\n  }, 3400);\n  tl.add({\n    targets: ['#roll-left', '#roll-right'],\n    opacity: [1, 0],\n    duration: 400, easing: 'easeInOutSine',\n  }, 3500);\n\n  /* PHASE D — FRAME + GRID DRAW (3300–4700) */\n  tl.add({ targets: '#grid-group',   opacity: [0, 0.9], duration: 800,  easing: 'easeOutQuart' }, 3300);\n  tl.add({ targets: '#frame-rect',   strokeDashoffset: [anime.setDashoffset, 0], duration: 1300, easing: 'easeInOutQuad' }, 3400);\n  tl.add({ targets: '#frame-detail', opacity: [0, 1], duration: 600, easing: 'easeOutCubic' }, 4200);\n  tl.add({ targets: '#wf-tags',      opacity: [0, 0.55], duration: 600, easing: 'easeOutCubic' }, 4400);\n\n  /* PHASE E — DRAW SCHEMATIC (4400–7100) */\n  tl.add({\n    targets: '.bh-line',\n    strokeDashoffset: [anime.setDashoffset, 0],\n    delay: anime.stagger(28),\n    duration: 700,\n    easing: 'easeInOutQuad',\n  }, 4400);\n\n  /* PHASE F — BROWSER MORPH (6400–8200) */\n  tl.add({\n    targets: '#paper-sheet',\n    fill: ['#FFFFFF', '#F7FAFF'],\n    duration: 900, easing: 'easeInOutCubic',\n  }, 6400);\n  tl.add({\n    targets: ['#grid-group','#frame-rect','#frame-detail','#wf-tags'],\n    opacity: [1, 0], duration: 700, easing: 'easeInOutCubic',\n  }, 6400);\n  tl.add({\n    targets: '.bh-line',\n    opacity: [1, 0], duration: 700, easing: 'easeInOutCubic',\n  }, 6600);\n  tl.add({ targets: '#browser-ui', opacity: [0, 1], duration: 200, easing: 'linear' }, 6400);\n  tl.add({ targets: '#bx-chrome',  opacity: [0, 1], duration: 800, easing: 'easeOutCubic' }, 6700);\n  tl.add({ targets: ['#bx-dot1','#bx-dot2','#bx-dot3'], opacity: [0, 1], delay: anime.stagger(80), duration: 380, easing: 'easeOutCubic' }, 6950);\n  tl.add({ targets: '#bx-addr',      opacity: [0, 1], duration: 500, easing: 'easeOutCubic' }, 7050);\n  tl.add({ targets: '#bx-addr-text', opacity: [0, 0.85], duration: 500, easing: 'easeOutCubic' }, 7200);\n  tl.add({ targets: '#bx-content',   opacity: [0, 1], duration: 900, easing: 'easeOutCubic' }, 7050);\n\n  /* PHASE G — MONITOR MORPH (7200–8750) */\n  tl.add({\n    targets: '#mon-bezel',\n    opacity: [0, 1], strokeWidth: [0, 6],\n    duration: 800, easing: 'easeInOutCubic',\n  }, 7200);\n  const neck = document.getElementById('mon-neck');\n  const neckState = { s: 0 };\n  tl.add({\n    targets: neckState, s: [0, 1],\n    duration: 650, easing: 'cubicBezier(0.4, 0, 0.2, 1)',\n    update: () => {\n      neck.setAttribute('transform', `translate(0 0) scale(1 ${neckState.s})`);\n      neck.style.transformOrigin = '800px 720px';\n    },\n  }, 7400);\n  tl.add({ targets: '#mon-neck', opacity: [0, 1], duration: 200, easing: 'linear' }, 7400);\n\n  const base = document.getElementById('mon-base');\n  const baseState = { s: 0 };\n  tl.add({\n    targets: baseState, s: [0, 1],\n    duration: 650, easing: 'cubicBezier(0.4, 0, 0.2, 1)',\n    update: () => {\n      base.setAttribute('transform', `translate(0 0) scale(${baseState.s} 1)`);\n      base.style.transformOrigin = '800px 767px';\n    },\n  }, 7750);\n  tl.add({ targets: '#mon-base',  opacity: [0, 1], duration: 200, easing: 'linear' }, 7750);\n  tl.add({ targets: '#mon-shadow', opacity: [0, 0.16], duration: 700, easing: 'easeOutCubic' }, 8050);\n  // (Stage-group lift removed for the same reason — keeps composition fixed.)\n\n  // ── PHASE J — RESPONSIVE: PC METAMORPHS INTO PHONE ──\n  // All sub-steps are driven directly by scroll progress in update() below.\n  // The responsive phase holds, scrolls the drawn phone, then hands off to DOM.\n  const flipState = { angle: 0, scale: 1 };\n  const applyFlip = () => {\n    const a = flipState.angle, s = flipState.scale;\n    const t = `translate(${800*(1-s)} ${450*(1-s)}) scale(${s}) rotate(${a} 800 450)`;\n    document.querySelectorAll('[data-flip=\"1\"]').forEach(el => el.setAttribute('transform', t));\n  };\n\n  /* Scroll scrubbing */\n  const TOTAL = 10000;\n  let lastRenderedP = null;\n  let targetP = 0;\n  let renderedP = 0;\n  let rafId = 0;\n  let didInitialRender = false;\n  function getProgress() {\n    const r = section.getBoundingClientRect();\n    const viewport = window.visualViewport ? window.visualViewport.height : window.innerHeight;\n    const scrollable = Math.max(1, section.offsetHeight - viewport);\n    return Math.max(0, Math.min(1.28, -r.top / scrollable));\n  }\n  // Decouple the desktop→phone flip from raw timeline scrubbing so the bezel\n  // is never caught at a mid-rotation tilt: do the morph in a tight scroll\n  // window inside the final phase, snap to final state past it, snap to identity\n  // before it. This also guarantees a clean rewind back to the blueprint —\n  // any leftover transform from a previous scroll gets reset every frame.\n  const FLIP_IN  = 0.910;  /* phone morph starts after the responsive note has room to breathe */\n  const FLIP_OUT = 0.970;  /* slower turn, then a visible settle before zoom */\n  /* Hero intro overlay — fades out as the animation begins */\n  const heroIntroEl = document.getElementById('hero-intro');\n  const navEl = document.querySelector('.nav');\n\n  // Cached phase-J element groups (opacity driven directly from p in update()).\n  const monStand = document.querySelectorAll('#mon-neck, #mon-base, #mon-shadow');\n  const bxChrome = document.querySelectorAll('#bx-chrome, #bx-dot1, #bx-dot2, #bx-dot3, #bx-addr, #bx-addr-text');\n  const bxDesk   = document.getElementById('bx-content');\n  const bxMob    = document.getElementById('bx-mobile');\n  const heroHud  = document.querySelectorAll('.bh-hud-top, .bh-hud-bottom, .bh-side-tag');\n  const phaseHud = document.querySelectorAll('.ph-label, .ph-index');\n  const phoneDesign = document.getElementById('phone-design-site');\n  const phoneLive = document.getElementById('phone-live-site');\n  const phoneScrollThumb = document.getElementById('phone-scroll-thumb');\n  const phNotch  = document.getElementById('ph-notch');\n  const handoffSite = document.getElementById('handoff-site');\n  const studioSection = document.getElementById('inicio-positioning') || document.getElementById('studio');\n  const studioMain = document.querySelector('#inicio-positioning .home-grid:first-child') || document.querySelector('#studio .studio__inner:first-child');\n  const ramp = (p, a, b) => clamp01((p - a) / (b - a));\n  const setOp = (els, v) => { els.forEach(el => el.style.opacity = v); };\n  const setVar = (els, name, v) => { els.forEach(el => el.style.setProperty(name, v)); };\n  const smooth = v => v * v * (3 - 2 * v);\n  const soft = v => v * v * v * (v * (v * 6 - 15) + 10);\n  const lerp = (a, b, t) => a + (b - a) * t;\n  const morphRect = (id, from, to, t) => {\n    const el = document.getElementById(id);\n    if (!el) return;\n    el.setAttribute('x', lerp(from.x, to.x, t));\n    el.setAttribute('y', lerp(from.y, to.y, t));\n    el.setAttribute('width', lerp(from.w, to.w, t));\n    el.setAttribute('height', lerp(from.h, to.h, t));\n    if (from.rx !== undefined && to.rx !== undefined) el.setAttribute('rx', lerp(from.rx, to.rx, t));\n  };\n  function applyPhoneScreenScroll(p, rawP) {\n    const t = soft(ramp(p, 0.958, 0.992));\n    morphRect('pm-media',\n      { x:704, y:252, w:192, h:104, rx:4 },\n      { x:704, y:232, w:192, h:54,  rx:5 }, t);\n    morphRect('pm-title-1',\n      { x:704, y:388, w:172, h:9,  rx:2 },\n      { x:704, y:288, w:188, h:18, rx:2 }, t);\n    morphRect('pm-title-2',\n      { x:704, y:404, w:142, h:9,  rx:2 },\n      { x:704, y:314, w:176, h:18, rx:2 }, t);\n    morphRect('pm-title-3',\n      { x:704, y:420, w:118, h:9,  rx:2 },\n      { x:704, y:340, w:150, h:18, rx:2 }, t);\n    morphRect('pm-body-1',\n      { x:704, y:444, w:178, h:4, rx:2 },\n      { x:704, y:376, w:186, h:5, rx:2.5 }, t);\n    morphRect('pm-body-2',\n      { x:704, y:454, w:154, h:4, rx:2 },\n      { x:704, y:390, w:164, h:5, rx:2.5 }, t);\n    morphRect('pm-cta',\n      { x:704, y:478, w:92, h:18, rx:9 },\n      { x:704, y:426, w:92, h:18, rx:9 }, t);\n    morphRect('pm-card-1',\n      { x:704, y:524, w:192, h:38, rx:5 },\n      { x:704, y:482, w:192, h:48, rx:5 }, t);\n    morphRect('pm-card-1-accent',\n      { x:716, y:538, w:38, h:5, rx:2.5 },\n      { x:716, y:496, w:42, h:5, rx:2.5 }, t);\n    morphRect('pm-card-1-line',\n      { x:716, y:550, w:128, h:6, rx:3 },\n      { x:716, y:508, w:136, h:6, rx:3 }, t);\n    morphRect('pm-card-2',\n      { x:704, y:574, w:192, h:38, rx:5 },\n      { x:704, y:544, w:192, h:48, rx:5 }, t);\n    morphRect('pm-card-2-accent',\n      { x:716, y:588, w:38, h:5, rx:2.5 },\n      { x:716, y:558, w:42, h:5, rx:2.5 }, t);\n    morphRect('pm-card-2-line',\n      { x:716, y:600, w:128, h:6, rx:3 },\n      { x:716, y:570, w:136, h:6, rx:3 }, t);\n    const playFade = 1 - ramp(p, 0.962, 0.978);\n    const playCircle = document.getElementById('pm-play-circle');\n    const playIcon = document.getElementById('pm-play-icon');\n    if (playCircle) playCircle.style.opacity = playFade;\n    if (playIcon) playIcon.style.opacity = playFade;\n    if (phoneScrollThumb) {\n      phoneScrollThumb.setAttribute('y', lerp(260, 350, t));\n      phoneScrollThumb.style.opacity = 0.78 * (1 - soft(ramp(rawP, 0.988, 0.998)));\n    }\n    if (phoneDesign) {\n      phoneDesign.setAttribute('transform', 'translate(0 0)');\n      phoneDesign.style.opacity = 1 - soft(ramp(rawP, 0.994, 1.000));\n    }\n    if (phoneLive) {\n      phoneLive.setAttribute('transform', 'translate(704 248)');\n      phoneLive.style.opacity = 0;\n    }\n  }\n  function applyStudioHandoff(rawP) {\n    const reveal = soft(ramp(rawP, 0.993, 0.998));\n    const settle = reveal;\n    if (handoffSite) {\n      handoffSite.style.opacity = 0;\n      handoffSite.style.setProperty('--handoff-morph', '0');\n      handoffSite.style.setProperty('--handoff-settle', '0');\n      handoffSite.style.setProperty('--handoff-y', '18px');\n      handoffSite.style.setProperty('--handoff-scale', '0.992');\n      handoffSite.style.setProperty('--handoff-head-opacity', '0');\n      handoffSite.style.setProperty('--handoff-head-y', '16px');\n      handoffSite.style.setProperty('--handoff-copy-opacity', '0');\n      handoffSite.style.setProperty('--handoff-copy-y', '18px');\n      handoffSite.style.setProperty('--handoff-clip', '48%');\n      handoffSite.style.setProperty('--handoff-skeleton-opacity', '0');\n      handoffSite.style.setProperty('--handoff-skeleton-y', '0px');\n    }\n    if (studioSection) {\n      studioSection.style.opacity = reveal;\n      studioSection.style.transform = `translateY(${lerp(18, 0, settle)}px)`;\n      studioSection.style.setProperty('--studio-handoff', String(reveal));\n      studioSection.classList.toggle('is-active', rawP >= 0.998);\n    }\n    if (studioMain) {\n      const lift = window.innerWidth < 820 ? 10 : 14;\n      studioMain.style.transform = `translate3d(0, ${lerp(lift, 0, settle)}px, 0)`;\n    }\n  }\n  function applyStageZoom(rawP) {\n    if (!stageGroup) return;\n    const zt = soft(ramp(rawP, 0.972, 1.000));\n    const mobile = window.innerWidth < 820;\n    const zoom = 1 + zt * (mobile ? 6.45 : 8.05);\n    const focusX = 800;\n    const focusY = 450 + zt * (mobile ? -116 : -132);\n    stageGroup.setAttribute(\n      'transform',\n      `translate(800 450) scale(${zoom}) translate(${-focusX} ${-focusY})`\n    );\n    return zt;\n  }\n  function renderHero(rawP) {\n    if (lastRenderedP !== null && Math.abs(rawP - lastRenderedP) < 0.0001) return;\n    lastRenderedP = rawP;\n    const p = clamp01(rawP);\n    frameSvgForViewport();\n    const time = TOTAL * p;\n    tl.seek(time);\n    syncRevealClip(time);\n    applyStudioHandoff(rawP);\n    const hudIn = soft(ramp(p, 0.105, 0.135));\n    const hudOut = 1 - smooth(ramp(p, 0.835, 0.875));\n    setOp(heroHud, hudIn * hudOut);\n    setVar(phaseHud, '--phase-hud-opacity', String(1 - soft(ramp(p, 0.970, 0.990))));\n    if (p > 0.90 && paperSheet) {\n      paperSheet.setAttribute('fill', cssVar('--phone-screen') || cssVar('--bg') || '#071A33');\n    }\n    const svgOut = soft(ramp(rawP, 0.986, 0.996));\n    if (stageGroup) stageGroup.style.opacity = 1 - svgOut;\n    if (p <= 0.99 && paperSheet) paperSheet.setAttribute('stroke-opacity', '0.92');\n    if (p <= 0.99 && monBezel) monBezel.style.opacity = '';\n\n    /* Hero intro fades out as scroll begins (0 → 0.05) */\n    if (heroIntroEl) {\n      const introOpacity = 1 - ramp(p, 0, 0.05);\n      heroIntroEl.style.opacity = introOpacity;\n      heroIntroEl.style.pointerEvents = introOpacity > 0.05 ? 'auto' : 'none';\n    }\n    if (navEl) {\n      const phoneFocus = soft(ramp(rawP, 0.940, 0.965)) * (1 - soft(ramp(rawP, 0.994, 0.999)));\n      navEl.style.opacity = 1 - phoneFocus;\n      navEl.style.pointerEvents = phoneFocus > 0.7 ? 'none' : 'auto';\n    }\n\n    // ── Phase J sub-steps driven by scroll progress ──\n    // Only override after the monitor fade-ins have finished,\n    // so the timeline's fade-in path is left untouched on the way in.\n    if (p > 0.86) {\n      // 1) Monitor stand fades out well before rotation begins.\n      setOp(monStand, 1 - soft(ramp(p, 0.895, 0.942)));\n      // 2) Chrome strip fades out together with the stand.\n      setOp(bxChrome, 1 - soft(ramp(p, 0.900, 0.948)));\n      // 3) Desktop website content fades out just before rotation.\n      bxDesk.style.opacity = 1 - soft(ramp(p, 0.905, 0.955));\n      // tl.seek() naturally restores these to their timeline values when\n      // scrolling backward past this range, so no explicit reset is needed.\n    }\n    // 4) Rotation (FLIP_IN..FLIP_OUT) — smoothstep, with snap before/after.\n    let ft;\n    if      (p <= FLIP_IN)  ft = 0;\n    else if (p >= FLIP_OUT) ft = 1;\n    else {\n      const t = (p - FLIP_IN) / (FLIP_OUT - FLIP_IN);\n      ft = t * t * (3 - 2 * t);\n    }\n    flipState.angle = 90 * ft;\n    flipState.scale = 1 - 0.55 * ft;\n    applyFlip();\n    // 5) Mobile content appears, scrolls from design into the live site.\n    bxMob.style.opacity = soft(ramp(p, 0.952, 0.974)) * (1 - soft(ramp(rawP, 0.994, 1.000)));\n    applyPhoneScreenScroll(p, rawP);\n    // 6) Status notch fades in last.\n    phNotch.style.opacity = soft(ramp(p, 0.970, 0.988));\n    // 7) Final movement: zoom into the phone website instead of scrolling past it.\n    const zoomT = applyStageZoom(rawP) || 0;\n    if (zoomT > 0) {\n      const frameOpacity = 1 - soft(ramp(rawP, 0.988, 1.000));\n      if (monBezel) monBezel.style.opacity = frameOpacity;\n      if (paperSheet) {\n        paperSheet.setAttribute('stroke-opacity', String(0.92 * frameOpacity));\n      }\n      phNotch.style.opacity = soft(ramp(p, 0.970, 0.988)) * frameOpacity;\n    }\n    if      (p < 0.115) setPhase(-1);\n    else if (p < 0.24) setPhase(0);\n    else if (p < 0.38) setPhase(1);\n    else if (p < 0.54) setPhase(2);\n    else if (p < 0.70) setPhase(3);\n    else if (p < 0.800) setPhase(4);\n    else if (p < 0.985) setPhase(5);\n    else                setPhase(-1);\n  }\n\n  function tick() {\n    renderedP = targetP;\n    renderHero(renderedP);\n    rafId = 0;\n  }\n\n  function update() {\n    targetP = getProgress();\n    if (!didInitialRender) {\n      renderedP = targetP;\n      didInitialRender = true;\n      renderHero(renderedP);\n      return;\n    }\n    if (!rafId) rafId = requestAnimationFrame(tick);\n  }\n  if (typeof lenis !== 'undefined' && lenis) lenis.on('scroll', update);\n  window.addEventListener('scroll', update, { passive: true });\n  window.addEventListener('resize', update,  { passive: true });\n  update();\n})();\n\n/* ── 9. Horizontal case studies ── */\n(function(){\n  const track = document.getElementById('cases-track');\n  const bar   = document.getElementById('cases-bar');\n  if (!track) return;\n  if (REDUCED) return;\n  if (window.matchMedia('(max-width: 820px)').matches) return;\n\n  ScrollTrigger.create({\n    trigger: '.cases',\n    pin: '.cases__pin',\n    start: 'top top',\n    end: '+=280%',\n    scrub: 1.2,\n    onUpdate(self) {\n      const cards = track.querySelectorAll('.case-card');\n      if (!cards.length) return;\n      const gap = 28;\n      const cardW = cards[0].offsetWidth;\n      const total = (cards.length - 1) * (cardW + gap);\n      gsap.set(track, { x: -(total * self.progress) });\n      if (bar) gsap.set(bar, { scaleX: self.progress, transformOrigin:'left' });\n    }\n  });\n})();\n\n/* ── 11. Sticky pill ── */\n(function(){\n  const pill = document.getElementById('pill');\n  if (!pill) return;\n  pill.addEventListener('click', () => {\n    const target = document.querySelector('#contacto-preview');\n    if (target) target.scrollIntoView({ behavior: REDUCED ? 'auto' : 'smooth', block:'start' });\n    else window.location.href = 'mailto:hello@buildhausstudio.com';\n  });\n  ScrollTrigger.create({\n    trigger: 'body', start:'55% top', end:'93% top',\n    onEnter:     () => pill.classList.add('is-floating'),\n    onLeave:     () => pill.classList.remove('is-floating'),\n    onEnterBack: () => pill.classList.add('is-floating'),\n    onLeaveBack: () => pill.classList.remove('is-floating'),\n  });\n})();\n\n/* ── 12. Anchor scrolling ── */\ndocument.querySelectorAll('a[href^=\"#\"]').forEach(a => {\n  a.addEventListener('click', e => {\n    const id = a.getAttribute('href');\n    if (id.length < 2) return;\n    const target = document.querySelector(id);\n    if (!target) return;\n    e.preventDefault();\n    if (lenis) lenis.scrollTo(target, { duration:2.2 });\n    else target.scrollIntoView({ behavior:'smooth' });\n  });\n});";
